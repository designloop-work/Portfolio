[build]
  command = "npm run build"
  publish = "dist"

# Configure Netlify to redirect all routes to index.html for Single Page Applications (SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
